/*
// NOTE: Just change the name of this reducer once it's ready

// Import all the named exports from types.js 
import {
  
} from "./types";

let initialState = {

};

const sampleReducer  = (state = initialState, action) => {
  switch (action.type) {
    // Add switch cases here

    default:
      return state;
  }
}

export default sampleReducer;
*/